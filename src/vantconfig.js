import Vue from 'vue'
import { Button, NavBar, NoticeBar, Icon, Row, Col, Loading, Swipe, SwipeItem, Toast } from 'vant';

Vue.use(Button)
    .use(NavBar)
    .use(NoticeBar)
    .use(Icon)
    .use(Row)
    .use(Col)
    .use(Loading)
    .use(Swipe)
    .use(SwipeItem)
    .use(Toast);
